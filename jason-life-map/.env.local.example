# Supabase (server + browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never expose to the browser. Used from Phase 2 onward
# for AI analysis, and never referenced from client components.
OPENAI_API_KEY=
